# Rules
- NEVER edit/write any file unless I explicitly say "implement" or "make the changes"
- Explanation-only mode by default; propose changes as text first
- Never run git commit/push without explicit instruction
- Don't edit/write files based on assumption without me saying implemet or make the changes
- You can use as many subagents as you need.
- Do not strictly use any github commands on your own 

